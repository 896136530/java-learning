# 第2章 线性表 —— 练习题（无答案版）

> 做完喊「**检查 DS**」，我批改并给你逐题讲解。
> 做题方式：每题后面直接写答案（选择题 `1.C`，判定写 `O(n)` 这种），保存后喊我。

## 一、选择题（12 题）

1. 关于线性表，下列说法正确的是？
   A. 任意两个元素可以没有前后关系　B. 元素之间不要求有序
   C. 除首元素外每个元素有唯一直接前驱，除尾元素外每个元素有唯一直接后继　D. 相邻元素必须数值递增
   答：____

2. 在顺序表（数组）的中间位置插入一个元素，平均要移动多少个元素？
   A. 1　B. n/2　C. n　D. log n
   答：____

3. 链表插入删除比顺序表快，根本原因是？
   A. 链表更短　B. 只需改指针，不用移动其他元素　C. 链表内存连续　D. 链表不需要遍历
   答：____

4. 在单链表中查找第 i 个元素的时间复杂度是？
   A. O(1)　B. O(log n)　C. O(n)　D. O(n²)
   答：____

5. 顺序表按下标取值 `lst[i]` 的时间复杂度是？
   A. O(1)　B. O(n)　C. O(log n)　D. O(n²)
   答：____

6. 用**头插法**依次插入 1,2,3 建立链表，最终链表顺序是？
   A. 1→2→3　B. 3→2→1　C. 2→1→3　D. 随机
   答：____

7. 一个程序要**频繁在中间插入和删除**元素，首选哪种存储？
   A. 链表　B. 顺序表　C. 数组　D. 都一样
   答：____

8. 一个程序要**频繁按下标随机访问**元素，首选哪种存储？
   A. 链表　B. 顺序表　C. 队列　D. 都一样
   答：____

9. Python 的 `list` 本质上是？
   A. 链表　B. 动态数组（顺序表）　C. 哈希表　D. 双向链表
   答：____

10. `lst.append(x)` 的摊还时间复杂度是？
    A. O(1)　B. O(n)　C. O(log n)　D. O(n²)
    答：____

11. `lst.insert(0, x)` 的时间复杂度是？
    A. O(1)　B. O(n)　C. O(log n)　D. O(n²)
    答：____

12. 删除单链表的首节点，正确的做法是？
    A. 把头指针指向原第二个节点　B. 先把首节点复制一份　C. 遍历到底　D. 首节点不能删
    答：____

## 二、复杂度判定（4 题，写出复杂度）

1. 
```python
for i in range(n):
    total += lst[i]
```
复杂度：____

2. 
```python
for i in range(n):
    lst.insert(0, i)     # 每次插到开头
```
复杂度：____

3. 
```python
# 在链表中找值为 v 的节点（只能从头一个一个走）
cur = head
while cur and cur.val != v:
    cur = cur.next
```
复杂度：____

4. 
```python
# 在有序数组中二分查找 v
lo, hi = 0, len(arr) - 1
while lo <= hi:
    mid = (lo + hi) // 2
    ...
```
复杂度：____