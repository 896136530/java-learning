# 第1章 绪论 —— 练习题（无答案版）

> 做完喊「**检查 DS**」，我批改并给你逐题讲解。
> 做题方式：在下面每题后面直接写你的答案（像 `1.C` 这样，写完保存），或直接在聊天里把答案发我，都行。

## 一、选择题（12 题）

1. 下列哪个属于数据的**逻辑结构**？
   A. 顺序存储　B. 链式存储　C. 树形结构　D. 索引存储
   答：____

2. 顺序存储结构的最大优点是？
   A. 插入删除快　B. 可按下标直接存取　C. 节省空间　D. 无需移动元素
   答：____

3. 链式存储相比顺序存储，主要优势是？
   A. 随机访问　B. 插入删除不必移动大量元素　C. 占用空间更少　D. 实现更简单
   答：____

4. ADT 的中文全称是？
   A. 抽象数据类型　B. 数据结构　C. 算法　D. 数据格式
   答：____

5. 算法要求"有穷性"，含义是？
   A. 代码行数有限　B. 执行有限步骤后必须结束　C. 内存占用有限　D. 输入数据有限
   答：____

6. 下列时间复杂度中增长最快（量级最大）的是？
   A. O(n)　B. O(log n)　C. O(n²)　D. O(2ⁿ)
   答：____

7. 两层嵌套循环（都执行 n 次）的时间复杂度是？
   A. O(2n)　B. O(n)　C. O(n²)　D. O(log n)
   答：____

8. 循环里每次都把规模减半（`n = n // 2`），时间复杂度是？
   A. O(n)　B. O(n²)　C. O(log n)　D. O(1)
   答：____

9. 只读取数组第一个元素 `lst[0]` 的操作，复杂度是？
   A. O(1)　B. O(n)　C. O(n²)　D. O(log n)
   答：____

10. 交换两个变量只用一个临时变量，空间复杂度是？
    A. O(1)　B. O(n)　C. O(n²)　D. O(log n)
    答：____

11. 蓝桥题 n 最大 10⁵，下面哪个算法**最稳妥**？
    A. O(n²)　B. O(n log n)　C. O(2ⁿ)　D. 三个都行
    答：____

12. 下列哪一项**不是**算法的五大特性之一？
    A. 有穷性　B. 确定性　C. 高效性　D. 可行性
    答：____

## 二、复杂度判定（4 题，写出每题复杂度）

1. 
```python
for i in range(n):
    x += 1
```
复杂度：____

2. 
```python
for i in range(n):
    for j in range(n):
        x += 1
```
复杂度：____

3. 
```python
while n > 1:
    n //= 2
```
复杂度：____

4. 
```python
for i in range(n):
    k = n
    while k > 1:
        k //= 2
```
复杂度：____

## 三、别忘了热身题

`T1求和.py`（n≤10⁹ 用公式还是循环？）和 `T2素数.py`（试到 √n）还在等你——做完一起发我检查。