# 📋 每日学习任务生成规范（新会话必读）

> 本文件是"交接手册"：换新对话/新 AI 时，先读本文件（含学习计划核心）+ `README.md`，细节再看 `java/学习路线.md` + `lanqiao/蓝桥杯备考路线.md`，即可无缝继续。

## 1. 我是谁 / 项目背景

- 河南大学 · 软件工程（数智空间方向）**大二学生**（英语/技术基础一般）
- 目标：Java 后端就业（主线）→ 大创 SpringBoot（大二下）→ 实习 → 秋招
- 蓝桥杯：**Python 大学 B 组**（**2027-04 省赛**，报名窗口 **2026-10~12** ⚠️），副线
- 学习记录仓库：`github.com/896136530/java-learning`（本地：`C:\Users\89613\Desktop\新建文件夹 (2)`，分支 master）
- 双线结构：`java/` = Java 就业主线；`lanqiao/` = Python 蓝桥杯竞赛线

## 1.5 学习计划核心（用户已确认的定稿，换对话必知）

> 完整版在 `java/学习路线.md`（Java 主线）和 `lanqiao/蓝桥杯备考路线.md`（Python 线）。以下是摘要，新 AI 必须照此规划后续任务。

### 总原则（用户原话确认）
- **"学校教不了什么，一切自学，不依赖学校课程"**——所有知识自学补齐
- **双线并行，Java 是基本盘、蓝桥杯是加分项**；时间冲突时 **Java 优先，Python 让位**
- 唯一交汇点：算法思维（二分/DP/搜索两线通用）
- **双线防混原则**：Java 学系统开发、Python 只写算法题——用途/场景分离，天然防混，别让 Python 学太宽（不学类/装饰器/爬虫）

### 出题难度/数量原则（用户确认过，生成任务时必须遵守）
- **时间增加 ≠ 题变多变难**。练习量只小增（Java 保持每天 5 题），**难度不动**——让知识点自然带难度（Day 递增）
- 多出来的时间用于"重做错题 + 复盘"（学习时间中约 70% 时间给复盘，30% 给新题）
- 过关标准只有一个：**不看答案能独立写出来 = 过关**；若"看答案才能写"的题变多 → 该回头复习，不是加难度
- 每周配 2~3 道 LeetCode 保持手感（Java 刷，蓝桥杯 Python 写）

### 学习节奏（用户 2026-09-12 拍板）
- **内容一道不减**（该学的必须学全）
- **简单章不拆**：基础/单一知识点的章节照旧**一天一章**（如 Day12 ArrayList、Day13 HashMap 基础）
- **难章才拆**：内容密度大、概念多的章（如 Day22 集合原理+排序、多线程、网络编程）可拆 **2~3 天**完成
- 一章 = 一个主题包（知识点 + 5 题 + Review）；用户分次写完即可，**不催进度、不缩内容**
- 批改**按题来**：用户写完几题就批几题（说"检查 DayN"就逐题真机验证），**剩下的下次接着写，已过的题不重做**
- 进度**看题数不看天数**：不记"第几天完成"，只记 Ex1~Ex5 / Review 的完成情况
- **禁止擅自改动用户正在写的当日任务文件**（用户 2026-09-12：Day22 快写完时要求"内容不要改"）

### Java 主线 8 阶段（工作日 2h + 周末半天；持续>强度）
| 阶段 | 时间 | 内容 |
|---|---|---|
| 一 | 现在~2026.10 | 语言基础 Day1~23（当前 Day23 综合实战·学生成绩排行榜）|
| 二 | 2026.9~10 | MySQL/JDBC/Maven/Git（JDBC 已于 Day21 上手）|
| 三 | 2026.10~12 | Spring/SpringBoot |
| 四 | 寒假（2027.1~2）~2027.3 | **数据结构跟学校课**（2026-09 已开课，跟课即可，不再自建线）+ 第一个项目（带登录/状态流转）+ **2027-03 底前写完大创材料**（避让 2027-04 蓝桥杯）|
| 五 | 2027 暑假 | SpringBoot 深入 + Redis + **Docker/MQ 必学（会用即可）** + 第二个项目部署上线 |
| 六 | 大三上（2027.9~2028.1）| **8 月（2027.8）就开始写简历盯招聘**，9 月投日常实习；八股：暑假看 JVM/并发、10 月起补计网/OS |
| 七 | 大三下（2028.2~6）| 暑期实习（大厂集中招）；4 月（2028-04）蓝桥杯第二次冲省一 |
| 八 | 大四（2028.9~11）| 秋招正式 offer |

### 数据结构（⚠️ 2026-09-20 更新：**学校课已开 + 老师每课留实验报告 → 不再自建跟课线**）
- **已删除 `datastructure/` 线**（原"第三条线：课本跟读 + Python 落地"）：用户明确说"老师每次课结束都会留实验报告，不需要再写那个了"。**今后不许再新建、不许再提这条线**
- 学校节奏：每周 3 小时理论 + 1.5 小时机房（AnyviewC 上机），**每课结束交实验报告**
- 我在这条课上的**唯一职责**：用户实验代码卡住时喊"检查实验N" → 真机编译+运行批改（如 2026-09-20 实验2 顺序表，见下方专条）
- 刷题侧维持不变：平时刷数组/字符串/模拟入门题；**链表/栈/队列/树/哈希/排序/DP 靠学校课程 + LeetCode 巩固**（不再自设"寒假系统补"计划）
- **蓝桥杯用 Python 写、LeetCode 用 Java 刷（一鱼两吃）**

### Python 蓝桥杯线（工作日 30~60min + 寒假集中）
- 时间线：Python 语法速刷 3~4 周（2026-09 起）→ 入门题（洛谷，每周 5~7 题）→ 基础算法（排序/二分/贪心/前缀和差分/DFS/BFS/背包DP，每专题 8~10 题）→ 寒假（2027.1~2）数据结构+近3年真题+每周 1 次 4h 限时模拟 → 2027-03 近 5 年真题+错题本+模板背诵 → 考前一周只翻错题本
- 目标策略：填空+简单题全对=省三稳，再解 1~2 道中等=省二（首年目标省二/省三）

### 关键日期（到点主动提醒！）
- ⏰ **2026-10~12：蓝桥杯报名**（学校不统一报名就自行去 lanqiao.cn 缴费）——错过等明年
- 🏆 2027-04：蓝桥杯省赛（大二下首次，目标省二/省三；2028-04 大三下第二次冲省一）
- 🎯 ~2027-03 底：大创申报材料写完（避让 2027-04 省赛）

### 其他已定决策
- 计网/OS：**用 AI 按需学，不指望学校课**（勿再推荐小林coding等静态资源，用户已选 AI 问答式）
- 算法题：LeetCode + 蓝桥杯，卡住就问 AI
- 期末周：考前 1~2 周降级为"每天 30 分钟保手感"，别挂科（绩点也是简历）

## 2. 每天生成任务的文件格式（严格照做）

### Java 任务 → 新建 `java/dayN/`（N = 下一数字）
| 文件 | 内容 |
|---|---|
| `知识点.md` | 当日概念 + "常见坑" + 与 Java/生活类比 |
| `练习题和代码题目.md` | 题目表格 + 每题详情 + 期望输出 + 💡提示 |
| `标准答案.md` | 全部答案（"做完再看"）|
| `DayN_Ex1.java` ~ `DayN_Ex5.java` | 模板：main 已固定，类留白 `// ===== 你的代码写在这里 =====` |
| `DayN_Review.java` | 完整可运行的综合示例（独立类）|

难度递进：题 4/5 是综合题（带校验/继承/方法重写等组合）。

### Python 任务 → 新建 `lanqiao/python-dayN/`
| 文件 | 内容 |
|---|---|
| `知识点.md` / `练习题和代码题目.md` / `标准答案.md` | 同上 |
| `Ex1_*.py` ~ `Ex5_*.py` + `Review.py` | 模板留白；重点教 input/split/map(int,)/f-string |

## 3. 质量红线（交付前必须做）

1. **答案必须本机编译/运行验证过**才能交付：
   - Java：`javac -encoding UTF-8` → `java -Dfile.encoding=UTF-8`（PowerShell 下要把参数用引号包住 `"-Dfile.encoding=UTF-8"`）
   - Python：`python`（机器有 3.9 和 3.13）
2. **用刁钻用例验证**（如存两次钱、负数、边界值、余额不足）
3. 题目和答案的类名/文件名必须一致可运行
4. 生成下一个任务前，先确认上一个任务作业已批改

## 4. 批改作业格式（用户要求）

```
表格（题号 | 结果 | 主要问题）+ 总体结论 + 具体建议
```
- 必须**先编译运行 + 刁钻用例验证**再下结论（用户作业可能有未保存/已改写情况，先重新 read 文件！）
- 只挑真问题说，夸做得好的点

### 批改分工（用户 2026-09-11 拍板，每天检查照此执行）
- **AI 直接代改**：语法/拼写/标点/格式/编译错误等**机械小错**（改完在报告里写明改了什么）
- **AI 只指路，用户自己改**：算法思路/逻辑错误（报位置 + 原因 + 提示，不代写，用户改完再验）
- **特殊例外**：整题未写等情况，先问用户要不要代补，**不擅自代写整段**（Day21 Review 曾由 AI 代补，仅此一次）

## 5. 交付流程

- **每次文件变动当场 `git add -A` → commit → push**（用户在意"我说上传了其实没推"，承诺过"你说上传我就当场推完再忙别的"）
- commit 标题中文，格式：`DayN 主题：要点`（Day5 曾因括号歧义被用户纠正，标题要清晰区分主题）
- README.md 的打卡进度表同步更新（Day 完成 → ✅）

## 6. 语言风格（硬性要求）

- **terse 中文**、多用表格、少术语；用户技术基础一般，解释要通俗（生活类比）
- 回复别太长，直给结论 + 下一步
- 用户催过"怎么不动了"——操作要连贯，别长时间沉默

## 7. 用户禁忌（有前科，务必注意）

- **删除/移动文件必须提前展示方案、等"行"再动**（曾误删文件夹/Junction 出事）
- 不要假设"学校有课"——用户明确**一切自学**
- 修改用户已写的代码前，先 read 最新文件（曾发生"批改的是旧版本"乌龙）
- Git commit 不能只 add 不 commit（用户两次发现"没推上去"）

## 8. 当前进度快照（最近更新 2026-09-22，换对话从这里接）

### Java 主线
- Day 1~6 ✅（变量/运算符/数组/方法/类对象/封装）
- **Day 7 继承 ✅**（extends/super/重写/protected，用户 5 题全部通过）
- 💡 **已定方案**：day7 各文件的辅助类名有重复（Ex1/Ex3/Review 都有 Animal、Dog；Ex2/Ex4 都有 Person、Student），**整目录一起 javac 会报"重复类"**——用户选择**每次单独运行一个文件**（VS Code 右上角 ▶ 只编译当前文件），无需改类名。若 VS Code 弹重复类错误，提醒"分开运行即可"
- **Day 8 多态 ✅（作业已批改通过，5 题全绿）**：向上转型/动态绑定/多态参数/多态数组/instanceof+向下转型全部掌握；批改时修正 Ex2 缺冒号、Ex3 用 Math.PI 导致输出 78.5398 与期望 78.5 不一致（改为 3.14 对齐，并提示实际项目用 Math.PI 更专业）。💡 已按"每题独立类名"设计，整目录可一起 javac（Day7 重复类问题在 Day8 已规避）
- **Day 9 接口 + 抽象类 ✅（作业已批改通过，5 题全绿）**：抽象类/接口/多接口实现/抽象类+接口组合/多态参数全部掌握；批改修复 Ex3 接口方法 Run 大写 R 编译错、Ex1/Ex5 把 sleep 改回父类普通方法、Ex4/Ex5 输出对齐。整目录可一起 javac
- **Day 10 Object 类 + 常用方法 ✅（作业已批改通过，5 题全绿）**：toString/equals/hashCode/getClass 全部掌握；批改修复 Ex4 多余 import（org.jcp 内部包不可见致编译失败）、Ex5 equals 补 instanceof 防 ClassCastException、知识点.md 标题笔误。整目录可一起 javac
- **Day 11 String 字符串 ✅（作业已批改通过，5 题全绿）**：equals 比较/长度字符/常用方法/字符统计/反转全部掌握；批改修复 Ex5 反转初始值写了 `" "`（带空格）导致前导空格+空串变空格的 bug。整目录可一起 javac
- **Day 12 ArrayList ✅（作业已批改通过，5 题全绿）**：动态数组/增删改查/遍历/边遍历边删大坑全部掌握；Ex2 初版 lastItem 误用 `remove`（把最后一个真删了，致"删除后还有 1 个"），用户自查自改 → get 后通过 ✅。当日连同 README 一并上传（经 API 蚂蚁搬家）
- **Day 13 HashMap ✅（作业已批改通过，5 题全绿）**：键值对/put 覆盖/get 返 null/containsKey/keySet 遍历/统计模板全部掌握；用户独立完成 5 题一次通过（查表/单词统计/改工资/过滤及格/投票）。当日连同 README 上传（API 蚂蚁搬家）
- **Day 14 其他集合 ✅（作业已批改通过，5 题全绿）**：HashSet 去重（Ex1 用 `add` 返回值计数，进阶用法）/Queue 叫号 poll+peek/LinkedList 头尾三件套/HashSet 交集/队列 poll 循环消费；亮点：Ex5 先缓存 `size()` 再进循环，躲开"边 poll 边判 size 少处理一半"大坑。当日连同 README 上传（API 蚂蚁搬家）
- **Day 15 异常 ✅（作业已批改通过，5 题全通）**：try/catch/finally 结构/Arithmetic/NumberFormat/越界/NPE 四大异常/多 catch 分流（越界与 NPE 分头接住）/循环容错累加全部正确；Ex4 小格式瑕：越界那行输出 `<越界>`（题目要 `越界`，套用了 Ex3 写法），已提醒、用户可改可不改。当日随 README 上传（API 蚂蚁搬家）
- **Day 16 泛型 ✅（作业已批改通过，5 题全通）**：泛型类 Box\<T\>/双泛型 Pair\<K,V\>/泛型方法 first、printAll、swap（临时变量交换）全部正确，菱形语法顺手用上；Ex4 小格式瑕：`print(s+" ")` 尾随空格（猫 狗 鸟 + 尾空格），已提醒、用户可改可不改。当日随 README 上传（API 蚂蚁搬家）
- **Day 17 IO 流 ✅（作业已批改通过，5 题全通）**：读首行/数行数/写文件/追加/复制全对；亮点：Ex5 先读进 ArrayList 再统一写出（不同于标答的边读边写，思路正确加分）；Ex1 用户把 main 里的相对路径改成了**绝对路径**（C:/Users/...）——能用但不可移植，已提醒"别改 main、cd 到项目根用相对路径"；Ex4 追加模式用得对（`FileWriter(path,true)`），但数据文件 notes.txt 缺结尾换行导致追加的 'c' 并进 'b' 变 'bc'——真实世界大坑（追加前确认原文件以换行结尾），已把数据文件补上换行；**配套数据文件 hello.txt/lines.txt/notes.txt/src.txt 已随作业入库**（check 时数据文件在运行目录自动重建，不依赖用户环境）。**Day 18 多线程 ✅ 已生成**（Thread 继承 vs Runnable 实现、start/run 区别、join、synchronized 原子性、sleep+受检异常、并发三特性；问候线程/双线程任务/同步计数器 1000/睡眠闹钟/卖票战 5 题 + 全家桶 Review，答案已注入验证：Ex1 join 后有序、Ex2 数量判定、Ex3 必须 1000、Ex4 叮咚×3、Ex5 卖出恰好 5 张余 0），**作业已批改通过（5 题全通，已上传）**：Ex1/Ex4 循环从 0 起（期望 1~3）小格式已提醒（读题习惯）；Ex2、Ex3 满分（synchronized 连跑 3 次全 1000）；Ex5 防超卖 2/2 次余 0（锁外判 `tickets!=0` + 同步 sell 双保险，能用；输出简化为"卖出一张票"未显剩余数，已提示）。**Day 19 多线程综合实战 ✅ 已生成**（分工-合并 merge 同步 / 共享对象锁方法 / ArrayList→synchronizedList 并发集合 / 下载进度 join / 6 窗口 100 张卖票 5 题 + 实战全家桶 Review，答案已注入验证：5050/4000/10000/进度100/余0），**Ex1~3 已批改通过（2026-09-06，全绿）**：Ex1 分段求和对（merge 带 synchronized、构造器 this.start 处理干净）；Ex2 银行取款 3 连跑全 4000（金额直接写死 1000 的简化版，锁加在同一个 Account 对象上=正确姿势）；Ex3 并发列表 3 连跑全 10000（synchronizedList 包对）。**Day19 五题全绿（2026-09-07，用户版已上传）**：Ex4 下载进度满分且**自带 volatile**（知识点梗用上了，加分）；Ex5 两处拼写（boolen→boolean、Runable→Runnable）代改后 3 连跑全余 0。**Day 20 网络编程 ✅ 已生成**（Socket：IP+端口/ServerSocket accept/Socket 连接/PrintWriter(true) 自动刷新/readLine 阻塞+null 断开/多线程服务器每连接一线程/面试三句话 TCP-UDP；回声/大写/加法/计数/多人 5 题 + 前缀回声全家桶 Review 自带代码，答案已注入验证 6/6：Ex1 服务器回话：你好，服务器、Ex2 HELLO、Ex3 3 + 4 = 7、Ex4 第1~3条：ping/pong/hi、Ex5 欢迎，小明！、Review [echo] hello/bye；**模板坑已修**：服务器无限 accept 循环线程必须 setDaemon(true)，否则 main 结束 JVM 不退出（首次验证曾超时））。**Day 20 作业 5/5 全绿（2026-09-10 复查上传）**：全部由用户自写；Ex5 两处标点代改（`欢迎：小明`→`欢迎，小明！`）；Ex1 用户自改对全角冒号后即 100% 通关、Ex4 全角冒号自带。**Day 21 JDBC ✅ 已生成上传**（MySQL 8.4.5 已本机安装：`E:\mysql`、服务 `MySQL84`(3306)、`root/root`、库 `day21` utf8mb4——zip 解压+my.ini+`--initialize-insecure`+UAC 装服务+改密+建库全程完成；知识=JDBC 五步/PreparedStatement 防注入/SQL 三件套/三坑+面试三句话；题=Ex1 连接/Ex2 建表/Ex3 插入/Ex4 查询/Ex5 更新/Review 全家桶；标准答案已注入**连真实 MySQL 验证 6/6**：连接成功、建表成功、插入3行成功、id=1 name=张三 score=88 三行、更新成功影响 1 行、Review 8 行全对；驱动 `lib/mysql-connector-j.jar` 不入库，编译 `javac -encoding UTF-8 -cp lib\mysql-connector-j.jar` + 运行 `java -cp ".;lib\mysql-connector-j.jar"`；踩坑：PowerShell `Set-Content -Encoding UTF8` 会写 BOM 头 javac 报非法字符——注入脚本必须 `[IO.File]::WriteAllText` 无 BOM）。
- **📒 项目模式启动（用户情绪点：嫌"光学知识没用、前面的内容忘了"，问什么时候写自己的项目）**：答复=现在就能写，遗忘靠"用"不靠重学；用户选定第一个 Java 小项目 **《命令行记账本》**（`java/项目-记账本/`，`任务书.md` + `Step1_记账本.java` 模板已生成，三步：Step1 HashMap 分类汇总(记一笔/查汇总/退出) → Step2 Bill 类+ArrayList 明细+异常防崩(金额输 abc 重来不崩)+删除一笔 → Step3 IO 持久化 账本.txt 启动读回）。**课目调整：项目与日课并行**（Day18 多线程已恢复生成、日课照常推进；记账本项目 Step1 模板已交付、等待用户完成，三步验收机制不变，Step1 未交前不生成 Step2）。 项目完成 = 简历"项目经历"素材 + 学 Spring 前的底子（大创 SpringBoot 预演）
- **⚠️ 上传经验（网络异常期，2026）**：git push 上传流被掐时（读通写断、RPC failed EOF），改用 **GitHub API 蚂蚁搬家**：逐文件 POST `/git/blobs`（小包不受影响；单文件超 ~10KB 成功率骤降，大文件可试 `"encoding":"utf-8"` 原文本代替 base64 缩小体积）→ POST `/git/trees`（用 base_tree 合并，路径必须完整 UTF-8）→ POST `/git/commits` → **PATCH** `/git/refs/heads/master`（PATCH 不是 POST）。三条铁律：① 禁 Invoke-RestMethod 传中文 body——它按 Latin-1 编码把非 ASCII 丢成 `?`；必须 [System.IO.File]::WriteAllText 写 UTF-8 JSON + curl --data-binary 原字节发送 ② tree 条目要**完整 40 位 SHA**（7 位短 SHA 会 422）③ blob 内容寻址，重传幂等可续传。上传后 `git fetch`+`git reset --soft FETCH_HEAD` 对齐本地（不碰工作区）。**④ 补充铁律（踩坑实录）**：refs PATCH 的成功响应**没有顶层 sha**（是 `object.sha`）——成功判定别只查 `.sha`，保险做法是 PATCH 后直接 `git fetch` 对比 FETCH_HEAD；手拼 JSON 出过 `400 Problems parsing JSON`——统一 **ConvertTo-Json 生成 + 本地 ConvertFrom-Json 先校验再发送**；上传脚本的诊断输出**一律 Write-Host**（Write-Output 会混进返回值，重试失败文本会污染结果导致"假成功"！）；**子树整体替换必须带上目录全部文件**（漏带 = 远程丢文件，已踩 python-day1 知识点.md、java-day14 四文件两次，替换前先 `ls-tree` 核对该目录完整清单）
- **Day 21 作业 6/6 全绿（2026-09-10，用户版已上传）**：Ex1 连接成功（用户自带 `Class.forName` 加分）；Ex2/Ex3 的 createTable 里多打印一行「建表成功」已代删（main 统一打印）；Ex4 用户自查补上 `executeUpdate()`（加分）后仅剩打印缺空格（`id=1name=张三score=88`）→ 已代改 printf；Ex5 一次通过；Review 用户未写（DBUtil 只有 TODO 注释）→ 已按用户既有代码风格（try-with-resources + Class.forName）补全全家桶（getConnection/createTable/insertStudent/queryAll/deleteById/updateScore/getUserCount）；复查连真库 6/6 全绿。数据库列类型用户写成 VARCHAR(50)/DOUBLE（题目模板 VARCHAR(20)/INT），能用但已提醒对齐标准。**Day21 运行方式坑（重要）**：VS Code ▶ 不带 lib jar（根目录已建 `.classpath`/`.project` 并挂 `java/day21/lib/mysql-connector-j.jar`，但 jdt.ls 项目导入一直没吃到，临时项目在 `%TEMP%\vscodesws_*\jdt_ws\jdt.ls-java-project`，无 .classpath 落盘）——用户改用 **`运行Day21.bat`**（day21 目录内，相对路径 javac/java + `lib\mysql-connector-j.jar`，双击或输题号 1~6；桌面快捷方式「Day21 运行器」已建，cmd /k 包装不关窗），实测通过。该 bat **不传 GitHub**（进 .gitignore）。**Day 22 集合框架全解 ✅ 已生成（2026-09-10）**（HashMap 原理=数组+链表+hashCode 定位桶/equals 判同、TreeMap 按键自动排序、Comparable compareTo vs Comparator compare 区别、复杂度选型表、遍历三件套；Ex1 HashMap 词频计数 getOrDefault/Ex2 ScoreBook 增改查（put 覆盖、Integer 返回防 NPE）/Ex3 TreeMap 学号升序/Ex4 Student Comparable 分数降序/Ex5 NameLenComparator 姓名长度+字典序/Review RankBoard 取 entrySet 按值重排序取前 3；纯 JDK 无数据库，答案注入已验证 6/6 全绿——Ex5 期望输出顺序吃 Unicode 字典序：李四→王五→张三丰→欧阳修（张 5F20 < 欧 6B27，3 字组张三丰在前））。**Day 22 作业闭环（2026-09-13）**：Ex1~Ex3 用户分次自写（09-12 夜）、Ex4~Ex5 用户自写（09-13）、**Review 由豆包（用户请的另一个 AI）代补** → 用户请豆包上传，两个提交（`4961509` Day22 6/6 + `da70ad4` python修复+规范更新）已推送，本地=远程=da70ad4；**AI 侧独立验收 = 6/6 全绿**（javac+java 逐字符比对通过）。⚠️ 已向用户指出 Review 风格非本人（lambda `list.sort` + 规范空格，与用户紧凑风格迥异），建议**自己重写一遍**（该题是 Day23 的地基）。Ex1 第 18 行残留多余成员变量（被局部变量遮蔽，不影响运行）已提醒用户自行决定是否删。
- **Day 23 综合实战·学生成绩排行榜 ✅ 已生成（2026-09-13）**：不学新语法，只做组装（类 + ArrayList + HashMap + TreeMap + Comparable/Comparator + entrySet + String.format）。知识点=零件清单/数据三种装法（对象·队伍·通讯录）/排行榜为什么不能用 TreeMap 直接排（同分覆盖）/五个常见坑/口诀"查得快用 Map，排队用 List，同分兜底加姓名，平均先转 double"。题：Ex1 Student 类（构造+toString+maxOf）/Ex2 TeamUtil 用 List 管全班（count/printAll/total）/Ex3 ScoreTable 按姓名查（Integer 防拆箱 NPE）/Ex4 **Comparable 复合比较**（分数降序 + 同分姓名升序兜底）/Ex5 分数段统计（Map 计数 + 档位从高往低判）/Review RankBoard 排行榜（entrySet→List→按 value 重排→前 3 + 平均分 `%.2f`）。**答案已注入验证 6/6 全绿 + 刁钻用例 3/3**（① 只有 2 人+同分：不越界且张三排李四前（张 5F20 < 李 674E）② 边界值 90/89/80/79/60/59 → 优秀1/良好2/及格2/不及格1 ③ 全部不及格：空档位输出 0 人不打 null）。期望基准：Ex1 张三 88·李四 95·最高分：95；Ex2 共 3 人·总分：259；Ex3 李四 95·赵六 无成绩·在册人数：3；Ex4 李四95·张三88·王五88·赵六76；Ex5 优秀2·良好1·及格2·不及格1；Review 第1名钱七100·第2名李四95·第3名张三88·平均分：83.80。**Day 23 批改（2026-09-16，六题全部用户自写）**：**Ex1 ✅**（张三 88 / 李四 95 / 最高分：95，独立目录也过）；**Ex2 ⚠️→✅**：① 机械错已代改——`printAll` 原来用 `System.out.print(s + " / ")` 打成一行，题目要四行，改成 `println(s)`；② **但缺 `class Student`（题目要求写，用户只写了 TeamUtil）**——现在能编过是因为同目录有 Ex1 编出的 `Student.class`（javac 隐式类路径），单独放空目录就报"找不到符号 类 Student"，已在报告指出并问是否代补（**未擅自代写**）；③ TeamUtil 里残留从 Student 抄来的 name/score 字段+构造+toString（提醒用户自定）；**Ex3 ✅**（李四 95 / 赵六 无成绩 / 在册人数：3，`Integer get` 用对了）；**Ex4 ❌ 逻辑错（只指路未代改）**：`Integer.compare(this.score, o.score)` 是**升序**、题目要**降序**——实测输出 `赵六76→张三88→王五88→李四95`（同分姓名升序兜底正确）；修法：参数顺序反过来 `Integer.compare(o.score, this.score)`；**Ex5 ✅**（优秀2/良好1/及格2/不及格1 全对），但 **map 写成了 `static` 类成员**——实测同进程调两次会累加（第二次 `优秀=4`），应把 map 声明在方法内部（已指路）；**Review ⚠️→✅**：① 机械错已代改——`list.get(i)` 直接打印 `Map.Entry` 会得到 `钱七=100`（`=` 而非空格），改为 `getKey() + " " + getValue()`；② 平均分 `83.80` 正确；③ 只指路未改：`for (i < 3)` 写死 3 → 人数不足会越界，建议 `Math.min(3, list.size())`。**⚠️ Day23 文件设计坑（重要）**：Ex1/Ex2/Ex4 都定义 `Student` → **整目录一起 javac 会互相覆盖 `.class`**（实测一起编后跑 Ex1 报 `NoSuchMethodError: Student.maxOf`），必须**一题一个文件单独编/单独 ▶**。**Day 24 已生成（2026-09-16）**（文件与异常实战·成绩单导入导出：内存vs硬盘 / 字节流vs字符流 / `readLine()` 读到 null / try-with-resources / `FileOutputStream(path, true)` 追加开关 / Windows 乱码必须 `StandardCharsets.UTF_8` / 受检异常 + catch 子类在前 / 返回空集合不返回 null / 空行 `NumberFormatException`；题=Ex1 写出成绩单、Ex2 读回并解析、Ex3 追加一行（漏 `true` 即覆盖）、Ex4 文件不存在也不崩、Ex5 解析+统计+筛选（数据故意含空行）、Review 成绩单导入导出 + Day23 排行榜复练；**改进：六题类名各自独立**（`ScoreWriter`/`ScoreReader`/`AppendUtil`/`SafeReader`/`ScoreStat`/`GradeBook`）→ **整个 day24 目录可一起 javac**，修掉 Day23 的 Student 覆盖坑；数据文件由程序自生成、不依赖外部；**答案注入模板端到端验证 6/6 逐字符一致**，内部基准：Ex1 `已写入 3 行/文件存在：true/文件非空：true`；Ex2 `共读到 3 行`+三行原文+`第一行解析：张三 考了 88 分`；Ex3 `最终 3 行`+`[张三,88, 李四,95, 王五,76]`；Ex4 `[提示] 文件不存在：no_such_file.txt`+`不存在文件读到 0 行`+`存在文件读到 3 行`；Ex5 `总分：364`+`及格 4 人`+四行名单；Review `已保存 5 人/读回 5 人/第1名：钱七 100/第2名：李四 95/第3名：张三 88/平均分：83.80`）。**Day 23 作业已按用户明确要求（2026-09-16 "行第23天的作业你直接帮我改吧"）由 AI 代改 4 处并复验通过**：① **Ex2 补 `class Student`**（原来靠同目录残留的 `Student.class` 侥幸编译，现在文件自包含）+ 删掉 `TeamUtil` 里从 Student 抄来的 name/score 字段与构造/toString；② **Ex4** 改为 `Integer.compare(o.score, this.score)`（降序修好）；③ **Ex5** map 从 `static` 类成员改成方法内局部变量（刁钻用例：同一进程连调两次结果完全相同，不再累加）；④ **Review** 改用 `Math.min(3, list.size())`（刁钻用例：只有 2 人时输出 2 行不越界、平均分 75.00）。**复验方式：每题放进独立空目录编译运行 + 逐字符比对，6/6 全绿。**⚠️ Day23 的 `Student` 重名坑仍在（Ex1/Ex2/Ex4 各有一份 `Student`）→ 只能一题一个文件单独编/单独 ▶，Day24 起已改为每题独立类名。**Day 24 批改（2026-09-16 晚，用户自写 Ex1~Ex5）**：**5/5 全绿**（每题放独立空目录编译运行、与期望逐字符一致）——Ex1 `ScoreWriter`（try-with-resources + UTF-8 ✓；多写了句 `pw.close()` 冗余无害；`throws Exception` 建议收窄为 `throws IOException`）；Ex2 `ScoreReader`（`readLine()` 判 null ✓、`return` 放 try 内 ✓）；Ex3 `AppendUtil`（`FileOutputStream(path,true)` 追加开关用对 ✓）；Ex4 `SafeReader`（catch 顺序子类在前 ✓、返回空列表 ✓）；Ex5 `ScoreStat`（自己抽了 `private static String[] split()` 复用、处理空行与段数不符 ✓，比提示还给得完整）。**Review 未写**（用户没动，留在原地）。⚠️ 用户把 Ex5 的 import 改成了 `java.io.BufferedReader/InputStreamReader`（该文件用不上，无害）。**Day 25 已生成（2026-09-16）**（MySQL 基础：库→表→行/列心智模型 / 四条语句 CRUD / 类型 INT·VARCHAR·DECIMAL·DATE / 约束 PRIMARY KEY·AUTO_INCREMENT·NOT NULL·DEFAULT / 查询四件套 WHERE·ORDER BY·LIMIT / GROUP BY + COUNT·AVG·MAX·ROUND / JOIN + ON / ⚠️ 最大坑 `UPDATE`·`DELETE` 忘写 WHERE 全表遭殃；题=Ex1 建库建表、Ex2 一条 INSERT 插三行、Ex3 条件查询、Ex4 更新删除、Ex5 分组聚合、Review 多表 JOIN；**形式与 Java 线一致**：每题 .sql = 上半段固定建表/塞数据 + 中间留白 + 底部验证语句；**答案注入模板端到端连真库验证 6/6 逐行一致**，内部基准：Ex1 `SHOW TABLES`→student、`DESC`→id PRI/auto_increment；Ex2 id1~3 张三88/李四95/王五76 + 人数3；Ex3 李四95/张三88；Ex4 一班 88/95/76→93/100/81（`score=score+5` 算出来的）、孙八(45)被删、剩 6 人；Ex5 一班 3 人 86.33/95、二班 2 人 80.00/100、三班 2 人 63.50/82；Review 连表 7 行（钱七100 李老师…）+ 班级人数 一班3/三班2/二班2（并列按班级名升序，三 U+4E09 < 二 U+4E8C）。**运行器 `java/day25/运行SQL.bat`**：列 .sql 菜单/拖拽执行 + 自动拉起 mysqld + 跑完 pause。**⚠️ 踩坑：.bat 必须 CRLF 换行**——该 bat 起初写成纯 LF（40 LF/0 CRLF），cmd 解析直接崩成 `'YSQL' is not recognized` 这类碎片（`set VAR=` 行被吃掉首字符），转 CRLF 后正常；顺手把 `C:\Users\89613\c-build\run-c.bat` 也转 CRLF 并复测（`test.cpp`→hello world 仍正常）。**本机 MySQL 现状**：8.4.5、库 `day21`+`day25`、root/root；服务 `MySQL84` 停止且非管理员起不来，本次由 AI 手动 `mysqld --defaults-file=E:\mysql\my.ini` 拉起（进程，非服务）供当天用。**Day 25 批改（2026-09-17 晚，用户自写 Ex1~Ex5）**：**Ex1 ✅**（建库建表全对，还多写了 `COMMENT` 中文注释=真实项目习惯，加分）；**Ex2 ✅**（一条 INSERT 插三行，id 自增 1/2/3 + 人数 3）；**Ex3 ✅**（`WHERE class_name='一班'AND score>=80` 紧贴没空格但 MySQL 能解析，输出 李四95/张三88 对）；**Ex4 ✅**（两条语句都带 WHERE，一班 88/95/76→93/100/81、孙八(45)被删、剩 6 人）；**Ex5 ⚠️ 数据对但漏写 ORDER BY（只指路未代改）**：写的是 `... GROUP BY class_name`（结尾连分号都没有），**没有 `ORDER BY AVG(score) DESC`**——本机输出恰好是一班/二班/三班（GROUP BY 的组顺序），看起来"对了"；**已做实验证明是巧合**：造一张"组顺序与平均分相反"的表（一班60/二班70/三班100），该写法输出仍是 一班/二班/三班，正确结果应是 三班/二班/一班 ✅（实验用完即 DROP）。另外 Ex5 没给 `class_name` 起中文别名（`AS 班级`）——非硬性要求，仅提醒。**（2026-09-17 深夜，用户"你帮我改吧然后上传" → AI 已代改 Ex5）**：补上 `ORDER BY AVG(score) DESC;`（含结尾分号）+ `class_name AS 班级` 别名，复验输出 `一班 3 86.33 95 / 二班 2 80.00 100 / 三班 2 63.50 82`；再用"组顺序与平均分相反"的 demo 表复验 ORDER BY 确实生效（三班100→二班70→一班60）。**Review 未写**。**Day 26 已生成（2026-09-17）**（JDBC 实战·成绩管理系统 v1：JDBC 五步复习 / `executeQuery` vs `executeUpdate`（查拿结果集、改拿行数）/ `ResultSet.next()` 游标 / ⭐`PreparedStatement` 的 `?` 与 SQL 注入对比（Ex1 埋了 `王五' OR '1'='1` 的注入尝试）/ 聚合下推（能 SQL 算的别搬回 Java）/ try-with-resources 管连接语句结果集 / URL 坑；题=Ex1 BasicDao（countAll + findScoreByName 返回 Integer）、Ex2 ParamDao（ResultSet→List）、Ex3 UpdateDao（参数化 UPDATE/DELETE 返回受影响行数）、Ex4 AggDao（ROUND(AVG) 下推）、Ex5 RankDao（`LIMIT ?` 参数化 + 同分姓名兜底）、Review ScoreService（insert/ranking/avgAll/countByClass 四方法，必须 LinkedHashMap 保 SQL 顺序）；**结构改进**：`Db.java`（连库工具）+ `Setup.java`（init/count/printAll 固定准备代码）作为**共享工具类**，六个文件各自 main 固定、类名唯一（`BasicDao`/`ParamDao`/`UpdateDao`/`AggDao`/`RankDao`/`ScoreService`）→ **整个 day26 目录可一起 javac**；**答案注入模板端到端连真库验证 6/6 逐字符一致**，内部基准：Ex1 `全校人数：7 / 李四：95 / 陈七：无成绩 / 注入尝试：无成绩`；Ex2 一班 李四95·张三88·王五76、三班 周九82·孙八45；Ex3 受影响行数 3 与 1、剩余 张三93/李四100/王五81/赵六60/钱七100/周九82；Ex4 一班86.33·二班80.00·三班63.50·全校78.00；Ex5 前3 钱七100/李四95/张三88、前5 再加 周九82/王五76；Review `插入成功，当前人数：8`、前3 钱七100/李四95/吴十90、全校 79.50、每班 一班3/二班3/三班2。**⚠️ Day26 两处真坑（都已修并复测）**：① **JDBC URL 的 `characterEncoding=utf8mb4` 非法**——`Unsupported character encoding 'utf8mb4'`（JDBC 要的是 **Java 字符集名 `UTF-8`**，连接器会自动映射到服务端 utf8mb4），6 个文件全崩在这，改 `characterEncoding=UTF-8` 后 6/6 通过；② **运行器菜单模式 bug**：`java ... %~n1` 在没有命令行参数（走菜单）时取到空主类名 → `用法：java [options] <主类>`，改成 `for %%f in ("%SRC%") do set "MAIN=%%~nf"` 后用 `%MAIN%`（day21 的 bat 因用"编号→类名"硬映射所以没这问题）；③ `运行Day26.bat` 同样**必须 CRLF**，已转并烟测（直接指定文件 + 菜单选 6 都跑通）。**⚠️ 环境**：MySQL 服务 `MySQL84` 仍需管理员才能起，`运行Day26.bat`/`运行SQL.bat` 都会在检测到 3306 未监听时自动 `mysqld --defaults-file=E:\mysql\my.ini` 拉起；本次为验证由 AI 手动起过（当前 3306 已在监听）。

- **Day 26 批改（2026-09-19，用户自写 Ex1~Ex5，Review 未写）✅ 五题全绿**：逐题编译运行，输出与期望**逐字符一致**；另加 **21 项刁钻用例**（空表 count、连续调用、不存在的人、注入字符串、查不存在的班、同分兜底、班级名打错返 0 行、负数扣分、删空、全班平均、全 0 分、limit 0/超总数/负数、连续调用一致）——**全部通过**。Ex1 用户把 `countAll` 也用 `PreparedStatement`（答案用 `Statement`），不算错：要练的是 `executeQuery` 取值的路子。**Ex5 的 `preparedStatement` 选型完全正确**（`LIMIT ?` 参数化 + 同分姓名升序兜底都在）。
  - **⚠️ Ex5 文件里有个明显"粘贴事故"（重要模式）**：写代码时把 TODO 注释块的最后几行**在后半段又粘了一遍**，导致第 33 行变成 `// TODO：static List<String> ranking(Connection conn, int limit)re DESC, name ASC LIMIT ?`、第 38 行变成 `//   · PreparedStatement：`、第 37 行排序说明又被搬到下面（3 行注释被搅成"错位循环"）。**代码本身无恙（注释而已）**，但模板很乱 → 已代整理成正常 TODO 块（机械错，规范允许）。**下次批改要留意这种"注释被搅乱"的形态**，别只看代码不看注释。
  - 仅 2 处**未代改的小瑕**（可改可不改，已提醒）：① Ex1 里 `"WHERE name=?"`、Ex3 里 `score=score+?`、`score<?` 等**运算符紧贴没空格**（MySQL 能解析，与 Day25 Ex3 同一个习惯）；② Ex3 SQL 关键字大小写混用（`Update` / `DELETE`）。
  - **⚠️ PowerShell 跑 java 的坑（复现并解决）**：`java -Dstdout.encoding=UTF-8 ...` 在 pwsh 下**未加引号会被吃掉** → `找不到或无法加载主类 .encoding=UTF-8` / `ClassNotFoundException: /encoding=UTF-8`。**必须写 `java "-Dstdout.encoding=UTF-8" "-Dstderr.encoding=UTF-8" -cp ".;<jar>" 类名`**（规范第 3 节早已记过，本次是复现确认）。
  - **⚠️ 沙箱写入限制**：本会话文件策略为 workspace-write，**系统临时目录 `%TEMP%` 不可写**（`Set-Content` 报 Access denied、javac `-d %TEMP%` 报写入失败）→ 所有校验用的临时源码/class 一律放**工作区内隐藏目录** `java/dayN/.checksrc/`（源码）与 `java/dayN/.check/`（class），跑完删除；两者已加进 `.gitignore`（`java/day*/.checksrc/`、`java/day*/.check/`）。

- **Day 27 已生成（2026-09-19）**（DAO 分层 + 事务：四层结构 main→DAO→实体→表 / 实体类 + `toString()` / `mapRow` 只写一次 / `addBatch`+`executeBatch`（返回 `int[]` 不是行数）/ ⭐**事务** `setAutoCommit(false)`→`commit()`/`rollback()`→`finally setAutoCommit(true)` + 转账事故演示 + ACID / **失败通知三种风格**（Ex4 抛异常、Ex5 返字符串、Review 返 boolean）/ 批量"打包送一趟"；题=Ex1 `Student`+`RowMapper`（实体 + 行映射，**公共零件必须写在 Day27_Ex1.java**）、Ex2 `BatchDao`（批量插入 3 人）、Ex3 `StudentDao`（多行→`List<Student>`，查不到返**空 List** 不返 null）、Ex4 `TransferService`（事务转账，**抛异常**风格，余额不足抛 `SQLException("余额不足")`）、Ex5 `SafeTransferService`（三道防线，**返字符串**风格，5 种失败原因）、Review `ScoreService`（ensureAccount 幂等 + addStudent/ranking/countByClass + 事务 transfer，**返 boolean**）；共享工具：`Db.java`（day27 库）+ `Setup.java`（init 建 student 7 人 + bank_account 张三/李四/王五 各 1000，另有 `printBanks`/`audit` 对账/`balanceOf` 查不到返 -1）；`Day27_Ex4.java` 内含 main 用的小工具 `openAccount`（已写好别改，给钱七/赵六开户，因为 Setup 里只有三个人）。
  - **答案注入模板端到端连真库验证 6/6 逐字符一致**，内部基准：Ex1 `张三 88 一班 / 分数：88 / 班级：一班 / 查不到的人：null`；Ex2 `插入前人数：7 / 插入行数：3 / 插入后人数：10` + 10 行学生；Ex3 `80 分以上：4 人`（钱七100·李四95·张三88·周九82）+ `200 分以上：0 人（不报错）`；Ex4 `初始：钱七 1000 赵六 1000 对账：5000 / 转 400 → 返回：true / 钱七：600 赵六：1400 / 对账：5000 / 转 99999 → 转账失败：余额不足 / 对账仍 5000`；Ex5 `失败：账户不存在 / 不能给自己转账 / 金额必须大于 0 / 成功：true`，转 200 后 张三800·李四1200·对账3000；Review `插入成功，当前人数：8`、前3 钱七100/李四95/吴十90、每班 一班3/二班3/三班2、转 400 → 钱七600 赵六1400 对账**5000**、转 99999 → false 且余额不变。
  - **刁钻用例 6/6 全过**：T1 同分兜底（王五 88 → 张三排王五前）；T2 删空表查 top 返 0 条；**T3 事务崩溃验证**（扣钱成功后故意抛异常 → rollback 把钱还回来，张三仍 1000、对账 3000）；T4 连续两次转 600（第一次成功 → 400，第二次"余额不足"）；T5 批量插空 List 返 0；T6 `ensureAccount` 连调两次仍只 1 条（幂等）。
  - **⚠️ 生成时的两处自纠**：① 我最初把 Ex4/Review 的对账写成 3000——**错**，`ensureAccount`/`openAccount` 加了钱七、赵六 各 1000 之后，银行总资产是 **5000**（学生账户 3000 + 新账户 2000），转账是"钱搬家"所以前后**必须都是 5000**，已全库改为 5000；② Ex5 的 TODO 注释我第一版把"为什么用 String 返回"的思路纠结写进了模板（很乱），已重写成干净提示。**教训：期望输出必须由真机跑出来再写进模板，不能手推**。
  - **⚠️ 模板验证口径**：六个模板未写代码时应当**编译失败**（`找不到符号`），已逐个确认——这才是"待练习"的正确状态。

- **Day 27 批改闭环（2026-09-21/22，用户自写 Ex1~Ex5，Review 未写）✅ 五题全绿**：真机连 MySQL 逐题编译运行，Ex1 `张三 88 一班 / 分数：88 / 班级：一班 / 查不到的人：null`、Ex2 `7→3→10 行 + 10 行学生`、Ex3 `80 分以上：4 人`、Ex4 对账 5000 守恒、Ex5 五道防线 + 转 200 → `成功：true / 张三800 / 李四1200 / 对账3000`，全部与期望一致。
  - **AI 代改机械错共 7 处**：Ex1 `public  static` 双空格 + 缩进、Ex2 `List<Student>list`/`int[] rows =ps` 空格、Ex4 `if (rs.next())` → `rs1`（`rs` 不存在）+ 补 `throws SQLException` + **删除半成品 `ps2` 块**（表名、参数、`executeQuery` 跑 UPDATE 三处都错）、Ex5 补 `throws SQLException` + 表名 `Account` → `bank_account` → 用户改成 `back_account`（少个 n）→ AI 再改为 `bank_account`。
  - **⭐ 用户唯一逻辑错（只指路未代改）**：Ex3 `if (rs.next() == false) return list;` 之后又 `while (rs.next())` —— **`rs.next()` 会移动游标**，第一次调用把第 1 行（钱七 100 二班）吃掉，实测 `80 分以上：3 人`（期望 4）。指路"`rs.next()` = 往下走一格 + 告诉你有没有踩到，判断和取值必须共用同一次调用"，用户自查删除 `if` 后复验通过 ✅。
  - **⚠️ 用户高频犯错模式（下次生成任务要在"检查清单"里预埋）**：① **方法签名漏 `throws SQLException`**（Ex4、Ex5 连栽两次，凡是碰 JDBC 的方法体就必查）② **SQL 表名/列名写错**（`Account`、`back_account`；编译期完全查不出，只在运行时报 `Table doesn't exist`）。
- **Day 28 已生成（2026-09-22）**（DAO 收口 + 事务隔离级别 + 连接池）：知识点=DAO 三条规矩 / **影响行数 0 不是异常** / `LAST_INSERT_ID()` / 聚合下推 / `LinkedHashMap` 保序 + **中文别当排序键** / **4 档隔离级别 + 脏读·不可重复读·幻读** / 乐观锁 vs 悲观锁 / **连接池原理 + 动态代理包装** / `LIMIT ? OFFSET ?` 分页必须 ORDER BY / 明天=Maven；题=Ex1 写操作三件套、Ex2 聚合下推 + topN + countByClass、Ex3 隔离级别实况（双连接）、Ex4 手写迷你池（`Proxy` 包壳）、Ex5 池化 vs 新建性能对比、Review 业务 DAO 合体（addAll 事务 + 分页 + transfer + 对账 4000）；共享 `Db.java`（day28 库，多一个 `newConnection()`）+ `Setup.java`（同 Day27 数据）+ `运行Day28.bat`（CRLF）。
  - **⚠️ 生成时自纠两处（真机验证才发现，模板/知识点/标准答案已全部同步）**：① `countByClass` 的 `ORDER BY COUNT(*) DESC, class_name` 实测得到 **`{一班=3, 三班=2, 二班=2}`**（中文按编码排，"三"<"二"）→ 改为 **`ORDER BY COUNT(*) DESC, MIN(id)`** 才是自然顺序；② 连接池"还回去再借"我原写 `c5 == c2` 为 **true**，实测**恒为 false**（**代理壳每次借时现做**，真连接复用而壳不复用）→ 已改为"看池子剩余数量判断复用"，并把"对象相等 ≠ 资源复用"写成知识点。**教训重申：期望输出必须真机跑出来，不能手推（第 3 次踩到）。**
  - 连接池选型说明：本机只有 `mysql-connector-j.jar`（无 HikariCP/Druid jar，不联网下载依赖）→ 故设计为**手写 20 行 MiniPool + Proxy**，反而更能讲清"借出的是壳"；Ex5 的复用判据也据此改为看池子数量。
  - Ex2 期望基准：`人数：7 / 平均分：78.00 / 最高分：100 / 前三 钱七100·李四95·张三88 / topN(10) 拿到 7 人 / {一班=3, 二班=2, 三班=2}`；Ex3：`REPEATABLE READ（可重复读）`、A 未提交时 A 看 700 **B 看 1000**、提交后 B 看 700、rollback 回到 700；Ex4：池 3 → 借 3 剩 0 → 第 4 个 null=true → 还 1 剩 1 → `c5 == c2` **false**；Ex5：30 次新建 **173ms** vs 池化 **66ms**、两个 `==` 都 false；Review：8 人 → 11 人 → 分页三页 → 删冯十三 10 人 → 转 300 true / 99999 false → `钱七：700 李四：1300 对账：4000`。
  - **未写清单（用户自愿，催不得）**：Day26/27 的 Review、python-day7 六个文件（已写完 4 个 Ex，Review/Ex5 待补）。

### Python 蓝桥杯线
- `lanqiao/python-day1/` ✅（print/变量/input/类型转换/split）——**已按用户要求去答案整改**：`标准答案.md` 已删，6 个模板清成纯占位（原来 Ex3/4/5/Review 直接给了完整代码，用户明确反对"练习题带答案"），题目文件只留行为描述 + 输入输出格式，**不写期望输出样例**
- **⚠️ Python 线铁律（用户拍板）**：**不生成答案文件、题目/模板不带答案**——蓝桥刷题要自己长本事；写完用户喊"检查"，批改流程同 Java（我运行对比期望输出，报告 ✅/❌，不剧透代码）。批改基准只记内部：Ex1=三行自我介绍(含名字)；Ex2=`我叫{name}，今年{age}岁，我有100.5元`；Ex3=输入 小张 → `你好，小张！欢迎来到Python世界`；Ex4=输入 3⏎5 → `两数之和：8`；Ex5=输入 `7 9` → `两数之和：16`；Review=输入 `12 8`⏎`30` → `应找零：10元`
- **Day1 批改结论（已按用户指示原样上传）**：Ex1 ✅（第1天写成"第一天"，小事）、Ex2 ✅（用户故意试两种打印法，实验加分）、Ex3 ✅（小提醒：input 提示语机考禁、`!` 半角）、Ex4 逻辑 ✅ / 格式 ⚠️（输出 `两数之和；8`，分号≠冒号）、Ex5 技能 ✅ / 格式 ⚠️（缺 `两数之和：` 前缀）、Review ✅（补上了、算对 10 元，带两个提示语）。**用户决定不改了，两处格式微瑕保留原样**（`两数之和；8` 分号≠冒号、Ex5 缺 `两数之和：` 前缀；机考格式注意点已提醒完毕，判分/复习时留意）
- **Day2 批改通过（6/6 全绿，已上传）**：Ex1 无提示全对（7→奇数/8→偶数）；Ex2 首查 59 档 `peint` 拼写错误 NameError 崩溃 → 用户自查修复，复查四档全对（92/85/62/59）；Ex3 15/55（for+while 两版都写，加分）；Ex4 内容对（`end=" "` 尾随空格，机考注意）；Ex5 首查输入格式错误（题目"n 行"写成"一行"）IndexError 崩溃 → 用户改读 n 行修复（残留 `最大的数:` 前缀+双提示语）；Review 18 / **9**（n=7 时 3+6=9，**修正原基准 7→6 笔误**）。共性提醒过：input() 提示语机考禁、输出别带前缀
- **Day3 已生成**（字符串方法 split/join/切片/反转 + 列表 append/sort/reverse/sum/min/max + 一行读取姿势；题目不带答案）。内部基准：Ex1 `abc`→`cba`；Ex2 `hello world python`→3；Ex3 `1 2 3 4 5`→15；Ex4 `3 1 4 2`→`4 3 2 1`；Ex5 `I love python`→`python`；Review `2⏎张三 88⏎李四 95`→`李四 95`。**作业已批改通过（5 题全通，已上传）**：Ex2 曾误粘贴知识点 join/map 示例覆盖（从存档恢复用户原代码）；Ex1 `input()` 带提示语混入输出、用户两次改未落盘（编辑器未存盘），已代删提示语（`s[::-1]` 为用户本人思路）；Ex4 排序方向 `reverse=True` 一次性改对、结尾 `end=" "` 尾空格已提醒用户可选改；Ex5 "先假设第一个最长再比"最标准写法。Review.py 已完成批改通过（平行双列表 + `nums.index(max_nums)` 找最高分，两用例全过；并列最高分时 index 取第一个=行为符合"输出一个"）——**Day3 全五题 + Review 全部闭环**。**Day4 已生成**（字典 dict：查 get/增改/删/遍历 items、词频统计套路 `d[w]=d.get(w,0)+1`；集合 set：add/remove/discard、并|交&差-、保序去重坑；5 题 + Review，无答案不写期望样例）。内部基准：Ex1 `apple`→苹果、`xyz`→没有这个词；Ex2 `a b a c`→`a 2 / b 1 / c 1`；Ex3 `3 1 2 2 3 4`→4；Ex4 A=`1 2 3` B=`2 3 4`→`4 / 2 / 1`；Ex5 `2⏎张三 88⏎李四 95⏎李四`→95、`王五`→没有此人；Review `2⏎张三 88⏎李四 95`→`李四 95⏎91.5`。**Day4 作业已批改通过（五题全绿，已上传）**：Ex1 提示语第三次踩坑、用户自查删净（我曾误把 `dog` 当查不到用例，实为字典内单词，已更正为 `xyz`）；Ex2 思路全对（get 计数套路照搬知识点）仅最后一行 print(整个字典) → 用户要求代改逐行 items 输出；Ex3 补 len；Ex4 本就正确（上次误报是测试换行符问题）；Ex5 get 默认值补上（None→没有此人）。Review.py 未写（模板留空，用户说可等）。**Day5 已生成**（函数与作用域：def/形参实参/默认参数/return/局部 vs 全局/global 三铁律/蓝桥主流程+函数姿势；5 题 + Review，无答案不写期望样例）。内部基准：Ex1 `小明`→`你好，小明！`；Ex2 `5`→120、`0`→1；Ex3 `7`→是素数、`8`→不是素数；Ex4 `3⏎a⏎b⏎c`→`1/2/3`；Ex5 `7 + 5`→12、`7 / 2`→3.5；Review `2⏎张三 88⏎李四 95`→`李四 95`。**Day5 作业已批改（2026-09-14，五题全部用户自写）**：Ex1 ✅（小明→你好，小明！；仅多一次无用调用 greet(name) 已提醒）、Ex2 ✅（5→120、0→1 边界正确；变量名 sum 遮蔽内置函数，仅风格提示）、Ex5 ✅（7+5→12、7/2→3.5）；**Ex3 ⚠️ 输出多一行**（函数内 print 中文 + `return n`，main 又 print 返回值 → 判别会看到数字多一行；属设计/逻辑问题按分工只指路不代改：应让函数 return True/False、打印交给 main）；**Ex4 ❌ 逻辑偏差**（题目要"调用次数"=输出 1/2/3 三行；用户写成 `count += len(temp)` 累加字符串长度且只在末尾 print 一次 → 只输出单个 3；指路：`def add(): global count; count += 1; return count` 无参，主流程循环 n 次、每行读一次并 print(add())）。Review.py 用户未写 → **按用户明确要求由 AI 填入参考答案**（打擂台 get_highest + 元组列表 + 元组解包；实测 2 用例通过：李四 95、并列取第一个 赵六 100），供用户写完自己版本后当复习看。**Ex3/Ex4 已按用户明确要求由 AI 代改（2026-09-14）并验证通过**：Ex3 修 3 处（① 函数改回 `return True/False`、打印交给 main ② 删掉错位的 `break`——它写在 if 外面导致循环只跑一轮 ③ 删掉 `return print(...)` 返回 None 的写法）→ 9/9 用例全过（7/8/2/9/1/97/999983/1000000/4）；Ex4 重写为无参 `jishu()`（`global count` 加一并 return）+ 主流程循环 n 次、每行读入后 print 一次 → 5/5 用例全过（含 n=0 空输出、含长字符串输入验证与内容无关）；随后全量复跑 Ex1~Ex5 + Review **全绿**。**Day 6 已生成（2026-09-14）**（二维列表与矩阵操作：列表的列表 / `m[行][列]` / 读矩阵标准姿势 / ⚠️`[[0]*m]*n` 浅拷贝大坑 / 三种遍历 / 矩阵四件套 / 蓝桥网格题+方向数组；题=Ex1 全元素求和、Ex2 每行之和、Ex3 最值及位置（行列从 1 起）、Ex4 转置（非方阵）、Ex5 两条对角线之和、Review 矩阵乘法（n×m 乘 m×p，建议抽 read_matrix 复用）；**参考解已私下验证 12/12 用例全过**：21 / 5 / -10、6|15、9 1 2 / 9 1 1 / 4 2 2、转置 1 4|2 5|3 6 与 7|8|9、对角 15 15 与 7 7、矩阵乘法 58 64|139 154；Python 线惯例**不配标准答案.md**，用户自行验证）。**Day 6 批改（2026-09-15，五题全部用户自写）**：Ex1 ✅（正常 2×3→21、边界 1×1→5、负数→-10 全对）、Ex2 ✅（2×3→6|15、3×2→2|5|-10，每换一行清零正确）、Ex3 ✅（左上角→9 1 1、右下角→9 2 2、全负数→-1 1 2，行列从 1 起正确）、Ex4 ✅（2×3→1 4|2 5|3 6，非方阵行列没写反；`[[0 for _ in range(n)] for _ in range(m)]` 推导式用对了，成功躲开 `[[0]*n]*m` 浅拷贝大坑）；**Ex5 ⚠️ 数值对/格式错 → 已代改（机械错）**：原写成两个 print 各占一行（15 与 15），题目要"一行两个数"，改 `print(sum1,sum2)`；另把变量名 `martix` 全部拼写修正为 `matrix`；复验 3/3（15 15 / 5 5 / 5 5）。**只指路未代改（逻辑/思路）**：Ex5 主对角线用双层循环 `if j==i` 再取 `matrix[i][i]`——结果对但白跑 n² 次，O(n) 的活写成 O(n²)，单层 `sum1 += matrix[i][i]` 即可；Ex1/Ex2 变量名 `sum` 遮蔽内置函数（风格提示，与 Day5 同款）；Ex4 变量名 `n_m`/`m_n` 极易混、行尾 `end=" "` 尾随空格（可选改）。**Review.py（矩阵乘法）用户明确表示留到复习时自写，未代补、未代写**。另：`tempCodeRunnerFile.py` 是 VS Code Code Runner 残留文件（内含早期 Ex1 的错误版本 `list(map(int(input().split())))`），已提醒，删除与否由用户决定。**Day 7 已生成（2026-09-15）**（排序与自定义排序：`sort()` vs `sorted()` + "返回 None" 大坑 / 默认排序与 Unicode 码点（中文非拼音）/ `key=` / lambda / ⭐元组 key 多关键字（负号实现"一降一升"混合方向）/ 稳定排序与两遍排序 / 蓝桥排行榜取前 k；题=Ex1 基础升降序、Ex2 单关键字按分数排名（不要求并列）、Ex3 多关键字（分数降序+姓名升序）、Ex4 自定义 key（长度升序+字典序）、Ex5 取前 k 大之和、Review 成绩排行榜（多关键字 + `第i名` + 平均分 2 位）；**参考解已私下验证 14/14 用例全过，内部基准**：Ex1 `3 1 4 1 5` → 升 `1 1 3 4 5` / 降 `5 4 3 1 1`（边界单个 `7` → `7`|`7`；负数 `-3 -9 0` → `-9 -3 0`|`0 -3 -9`）；Ex2 `张三 88/李四 95/王五 76` → `李四 95｜张三 88｜王五 76`（n=1 → `赵六 60`）；Ex3 加 `王五 88` 同分 → `李四 95｜张三 88｜王五 88｜赵六 76`（张 U+5F20 < 王 U+738B）；Ex4 `banana ant cat apple` → `ant cat apple banana`（仅 1 个单词 → `dog`）；Ex5 `5 3 / 10 5 -3 8 8` → `10 8 8` + `26`（k=n → `4 3 2 1` + `10`；k=1 且有重复 → `5` + `5`）；Review 4 人（张三88/李四95/王五88/赵六76）→ `第1名 李四 95｜第2名 张三 88｜第3名 王五 88｜第4名 赵六 76｜平均分：86.75`（n=2、60/90 → `平均分：75.00`，验证两位小数补零）；Python 线惯例**不配标准答案.md**，用户自行验证。**项目 记账本：Step1 ✅ 已验收通过（09-07 晚 23:20 提交）**——记一笔 containsKey 累加 + keySet 汇总遍历（Day13 统计模板正解）；验收场景 4/4（吃饭:-20 / 收入:100 / 菜单循环 / 再见！）；备注：汇总分隔符半角冒号 vs 任务书示例全角（可选改），乱输入防崩按任务书属 Step2 考核不计。**Step2 已生成**（Bill 类 + 金额 try-catch 重试 + 记入明细 + 查看明细全部/按分类 + 删除一笔，TODO 四处 A~D；任务书对 Step2 无验收示例，验收按功能清单判）
- 备考路线已升级为《Python 组 B 组按周备考计划》（2026-09 → 2027-04，三阶段：Python 基础 → 数据结构对齐严蔚敏 → 真题冲刺）

### 📘 数据结构线（原 datastructure/）—— **2026-09-20 已按用户要求删除**
- **删除原因（用户原话）**："老师每次课结束都会留实验报告，我不需要再写那个了"——即课本跟读线（知识点/Python落地/蓝桥考点三件套）与学校实验报告重复，无维护必要
- **已删除内容**：`datastructure/README.md`、`章节计划.md`、`第1章-绪论/`（知识点/练习题/蓝桥考点/Python落地/T1求和/T2素数）、`第2章-线性表/`（同名四件套），共 12 个文件；git 历史保留可查（`git log -- datastructure` 仍能翻到，只是工作区不再有）
- **连带清理**：`README.md` 结构图与"第三条线"整节、`蓝桥杯备考路线.md` 阶段②表述、`java/学习路线.md` 里"学校不学数据结构必须自学"的旧口径，全部改为"学校已开课、跟课即可"
- **⚠️ 今后铁律**：不要再提议恢复这条线、不要再生成"跟课三件套"；数据结构相关的产出只围绕**学校实验报告**（用户喊"检查实验N"时批改代码）

### 🧪 学校实验课（动手做 + 交报告，与 Java 主线并列）
- **实验1-集合运算**（C 语言，2026-09 中旬，在桌面 `实验1-集合运算/`）：`set.h`/`set.c`/`main.c` 结构
- **实验2-顺序表**（C 语言，2026-09-19~20，在桌面 `实验2-顺序表/`）：`seqlist.h` + `seqlist.c` + `main.c`，15 项菜单，教材严蔚敏 SqList
  - **2026-09-20 批改（用户要求"全帮我改"）**：① `seqlist.c` 第 16~24 行把 `.h` 里的宏/`ElemType`/`SqList` 又定义一遍 → **13 个 `conflicting types` 编译错**，删掉即可；② `MergeList` 两个 bug——`if(La.elem[i]<Lb.elem[j])` 与 `if(La.elem[i]>Lb.elem[j])` 写成**并列两个 if**（前者 `i++` 后后者又读 `La.elem[i]`，`i==length` 时**越界读堆内存**，实测结果里出现 `-2086862752` 垃圾值，同一输入时而正常时而乱码）+ **尾部两个 `while` 原样搬运没去重**（`[1,1]+[1]` → `1 1`）；③ `DisplayList` 格式不符（要求 `10, 20, 30`，原为空格分隔且末尾多空格、无换行；空表打成 `(（空表）)` 括号重复）
  - **修法（可复用）**：合并类问题→**所有写入收成一个 `Append(Lc,&k,val)`**（`if (*k==0 || Lc->elem[*k-1]!=val)` 才放），因为合并结果必定有序，**"只跟上一个放进去的比"就等于全局去重**（表内重复 + 跨表重复一起解决），复杂度 O(m+n)；`if/else if/else` 保证每个元素只判一次读一次
  - **验证**：`gcc -Wall -Wextra` 零警告；合并去重 12/12 用例全绿；15 项菜单全回归；桌面原地编译通过
  - **额外加的功能**：菜单 **16 随机填充测试数据**（`RandomFillList(SqList*,int)`，`srand(time(NULL))` 放循环外，追加 1~99 随机数到表尾且不清空原数据，内部循环调 `ListInsert`）+ 根目录 `编译运行.bat`（带 `-finput-charset/-fexec-charset=UTF-8` 防中文乱码，**必须 CRLF 行尾**）
  - **踩坑**：桌面目录在工作区外，写文件需 `danger-full-access`（2026-09-20 后本会话已改为该策略）；`main.c` 的 `scanf("%d%d")` 会吃掉下一行的第一个数（管道喂输入时插入数据要分两行写 `10` / `1 10`）；`bat` 里 `pause` 会吞 stdin，管道测试要补空行

### 其他
- **🚨 生成节奏铁律（2026-09-20 用户拍板，违规过两次）**：
  - **不许提前生成后一天的内容**。用户没写完当天的（例：Day7 没写），我做别的事就行——**别顺手把 Day8 生成出来**（用户原话："等会我第第七天的还没写的你生成后面的干嘛"）
  - 触发条件：**只有**用户说"生成 dayN / 继续下一天"，或当天作业已批改通过，才生成下一天
  - **逐日路线**看 `lanqiao/蓝桥杯备考路线.md` 里的「📅 逐日路线（Day 级）」表（Day8 常用库 → Day9 字符串进阶 → **Day10 前缀和差分** → Day11 双指针 → …）
  - **⭐ 刷题配题规则**：**Day 10 起**，生成每天 Python 内容时**必须顺带生成当天能刷的蓝桥风格真题 + 配套测试数据文件**，用户明确说"不用自己上网找"（用户原话）。Day8~Day9 仍为纯语法练习
  - 真题**自己按蓝桥风格出**（`web_search` 2026-09-20 起余额不足，且照搬网上原文有出处风险）；题目**不带答案**、不写期望输出样例，答案只留内部
  - **Day8 已在 2026-09-20 提前生成但按用户要求"先放着不提醒"**（用户选择：留着，等写到再打开）；仓库里已有 `lanqiao/python-day8/`（知识点 + Ex1~Ex5 + Review 模板，已真机验证）
- **📌 2026-09-20 会话要点（下次接续看这里）**：
  - **仓库结构变化**：删除 `datastructure/` 线（只留 java/ + lanqiao/ 两线），另加"🧪 学校实验课"条目（实验1 集合运算、实验2 顺序表，源码都在**桌面**、不入本仓库）
  - **Java 主线进度**：Day 25（聚合/分组）✅ 已批改提交；**Day 26（JDBC 巩固）✅ 5/5 全绿已提交**；**Day 27（DAO 分层 + 事务）🔄 已生成待练习**（6 题 + Review，含 `Db.java`/`Setup.java`/`运行Day27.bat`，答案内部基准已记录在"Java 主线"条目）
  - **项目-记账本**：Step1 ✅ 验收 / Step2 ✅ 验收（A~D 四处全过）/ **Step3 持久化 🔄 已生成待做**（`Step3_记账本.java` 五处 TODO A~E + `Step3任务书.md` + `Step3标准答案.md`；答案版已实测两轮：空账本不崩、读回续记、脏行容错、总账 100/-20/80）
  - **沙箱策略变化**：本会话文件策略从 workspace-write 改为 **danger-full-access**，桌面文件可直接读写；**审批提示已关闭**，不要再用 `sandbox_permissions` 申请授权
  - **Git**：远端 `https://github.com/896136530/java-learning.git`（分支 master，URL 内含 token）；`git push` 偶尔 TLS 断连 → 重试或加 `-c http.version=HTTP/1.1` 即可成功；PowerShell 下 `sh.exe signal pipe` 报错是无害噪音
  - **C 语言工具链**：MSYS2 gcc 16.2.0 可用，中文防乱码加 `-finput-charset=UTF-8 -fexec-charset=UTF-8`；ASan 运行时缺失（`-fsanitize=address` 链接失败），越界问题只能靠设计审查 + 构造用例定位
- 用户装了 ponytail 系列 skill（6 个）到 DSH 技能目录，待命状态，用户喊才启用
- 蓝桥杯报名窗口 **2026-10~12**、省赛 **2027-04**——到时间主动提醒用户别错过
- **2026-09-15 日期口径统一（重要，别再改回去）**：此前 `README.md` / `java/学习路线.md` / 本文件残留"2025 年口径"（写于用户大一）。现全库统一为：**蓝桥杯报名 2026-10~12、省赛 2027-04**（首战大二下，目标省二/省三；2028-04 大三下第二次冲省一）、**大创申报 2027-03~04**、里程碑表整体顺延一年（投日常实习 2027.9、暑期实习 2028.3~5、秋招 2028.9~11）。用户 2026-09 为**大二上**，毕业 2029-06。

## 9. 换新对话时，用户会这样说

> "帮我继续之前的学习，看 C:\Users\89613\Desktop\新建文件夹 (2) 的仓库，先读任务生成规范.md"

新 AI 照此文件 + README + 学习路线执行即可，无需本对话上下文。