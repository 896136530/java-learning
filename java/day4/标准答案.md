# Day 4 标准答案

> ⚠️ 先把《练习题和代码题目.md》里的题做完再翻这里！

## 题 1 答案（两数之和）
```java
public static int add(int a, int b) {
    return a + b;
}
```

## 题 2 答案（数组最大值）
```java
public static int getMax(int[] arr) {
    int max = arr[0];
    for (int i = 1; i < arr.length; i++) {
        if (arr[i] > max) max = arr[i];
    }
    return max;
}
```

## 题 3 答案（判断质数）
```java
public static boolean isPrime(int n) {
    if (n < 2) return false;          // 0 和 1 不是质数
    for (int i = 2; i <= n / 2; i++) {
        if (n % i == 0) return false; // 找到能整除的就不是质数
    }
    return true;
}
```

## 题 4 答案（方法重载）
```java
public static int area(int side) { return side * side; }     // 正方形
public static int area(int w, int h) { return w * h; }       // 长方形
public static double area(double r) { return 3.14 * r * r; } // 圆
```

## 题 5 答案（值传递实验）
```java
public static void changeNum(int x) { x = 999; }   // 基本类型：传副本，改了外面不变
public static void changeArr(int[] a) { a[0] = 999; } // 数组：传地址，改了外面也变
```

**运行结果**：
```
num 现在是：10
arr[0] 现在是：999
```

**为什么**：`num` 是基本类型，`changeNum` 里改的是它的副本，外面不变；
`arr` 是引用类型，传的是数组的地址，`changeArr` 顺着地址直接改了真身，所以外面变了。