# 题 1：基础排序（升序 + 降序）
# 输入：一行，若干个用空格隔开的整数
# 输出：两行 —— 第 1 行从小到大，第 2 行从大到小（每行数字之间用空格隔开）
# 提示：sorted(lst) / sorted(lst, reverse=True)；去掉方括号用 ' '.join(map(str, lst))
# 自测：只有 1 个数、本身已排好序、全是负数，各测一组

# ===== 你的代码写在这里 =====

# ===========================
