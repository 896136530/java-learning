# 🎯 复习：成绩排行榜（多关键字排序 + 平均分）
# 输入：
#   第一行一个整数 n
#   接下来 n 行，每行「姓名 分数」
# 输出：
#   前 n 行：第1名 姓名 分数、第2名 姓名 分数 ……
#            （名次从 1 开始；分数从高到低，同分按姓名从小到大——规则和 Ex3 一样）
#   最后一行：平均分：xx.xx  （保留两位小数）
#
# 提示：
#   ① 排序用今天的多关键字 key：key=lambda x: (-x[1], x[0])
#   ② 名次：for i, (name, score) in enumerate(排好的列表, start=1)
#   ③ 平均分：sum(分 for _, 分 in rows) / n，再用 f"{avg:.2f}" 格式化
#   ④ 建议把「排序并输出排行榜」和「算平均分」各写成一个函数（复习 Day5 的封装）
# 自测：两人同分、n=1、平均分刚好是整数（也要显示成 xx.xx）

# ===== 你的代码写在这里 =====

# ===========================
