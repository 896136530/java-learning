# 🎯 复习：回文检测器（清洗 + 统计，综合今天的全部内容）
# 输入：第一行一个整数 n；接下来 n 行，每行一个字符串（可能含空格、大小写、标点）
# 输出：n + 2 行
#   前 n 行：每行对应输入的一行，是回文输出 YES，否则 NO
#   倒数第 2 行：回文个数 x    格式：回文个数 x
#   最后一行：最长整理后长度 y  格式：最长整理后长度 y
# 规则（重要，别自己改）：
#   ① 判断前先"清洗"：只保留字母和数字（ch.isalnum()），并统一转小写（ch.lower()）
#   ② 清洗后是空串的行 → 算回文，输出 YES，长度按 0 算
#   ③ "最长整理后长度"用**清洗后**的长度比（不是原串长度）
# 提示：
#   把清洗单独写成函数 normalize(line)，返回清洗后的字符串（复习 Day5 的函数封装）
#   判断用 t == t[::-1]；最长长度用 max(...)，注意 n 行都要参与比较
# 自测：
#   A man, a plan, a canal: Panama → YES（清洗成 amanaplanacanalpanama）
#   hello → NO
#   !!! → YES（清洗后是空串）
#   Aa → YES（转小写后 aa）
#   No 'x' in Nixon → YES（清洗成 noxinnixon）

# ===== 你的代码写在这里 =====

# ===========================