# 题 6：旋转匹配（环形技巧）⭐ 加练
# 输入：第一行字符串 a；第二行字符串 b
# 输出：一行 —— b 能不能由 a 循环左移若干位得到？能输出 YES，否则输出 NO
# 提示：a 的所有循环移位都是 a + a 的子串！
#       ⚠️ 先判长度：len(a) == len(b)，长度不同直接 NO（想清楚为什么必须判）
#       然后判断 b in (a + a)
# 自测：abcde / cdeab → YES；abcde / cdeba → NO；abc / abc → YES；ab / ba → YES；
#       abc / abcd → NO；ab / abab → NO（长度不同，⭐ 这组最容易被漏掉）

# ===== 你的代码写在这里 =====

# ===========================