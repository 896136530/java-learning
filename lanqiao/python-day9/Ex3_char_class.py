# 题 3：字符分类统计
# 输入：一行字符串 s（可能含空格，也可能含中文）
# 输出：一行，四个整数用空格隔开 —— 大写字母个数 小写字母个数 数字字符个数 其他字符个数
# 提示：'A' <= ch <= 'Z' 这种连续比较很好用（或 ch.isupper()）
#       ⚠️ 中文算"其他"（不是字母）；空格也算"其他"
#       注意用 elif 串起来（一个字符只能属于一类，别写四个独立 if）
# 自测：abc → 0 3 0 0；ABC → 3 0 0 0；12345 → 0 0 5 0；!@#$ → 0 0 0 4；a A 1 ! → 1 1 1 4

# ===== 你的代码写在这里 =====

# ===========================