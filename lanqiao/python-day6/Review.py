# 🎯 复习：矩阵乘法（C = A × B）
# 输入：
#   第一行 n m           → 矩阵 A 有 n 行 m 列
#   接下来 n 行，每行 m 个整数（A）
#   再一行 m p           → 矩阵 B 有 m 行 p 列
#   接下来 m 行，每行 p 个整数（B）
# 输出：n 行，每行 p 个整数（结果矩阵 C，数字之间用空格隔开）
#
# 提示：
#   C[i][j] = A 的第 i 行 与 B 的第 j 列 对应相乘再求和
#   三重循环：for i → for j → for k: s += A[i][k] * B[k][j]
#   先把 C 建成全 0 的 n×p 矩阵（用推导式造，别用 * 复制）
#   建议把「读矩阵」写成函数 read_matrix(rows, cols)，两处复用（练 Day5 的函数封装）

# ===== 你的代码写在这里 =====

# ===========================
