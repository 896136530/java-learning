# 🎯 复习：成绩统计小工具（Counter + math + f-string 综合）
# 输入：第一行一个整数 n；第二行 n 个整数分数（0~100）
# 输出：三行
#   第 1 行：出现次数最多的分数及其次数，格式 分数 次数
#   第 2 行：最高分和最低分，格式 最高 最低
#   第 3 行：平均分，保留 2 位小数（用 f"{avg:.2f}"）
# 提示：
#   Counter(scores).most_common(1)[0] 拿 (分数, 次数)
#   max(scores) / min(scores)
#   avg = sum(scores) / n
#   ⚠️ n 可能只有 1 个；平均分是整数时也要显示成 xx.00
# 自测：n=1、所有人同分、平均分正好是整数

# ===== 你的代码写在这里 =====

# ===========================