# 题 3：两串找不同（Counter 相减）
# 输入：两行字符串，第一行长、第二行短
# 输出：一行 —— 第一行比第二行"多出来"的那些字符，按字典序从小到大拼成一串
# 提示：a = Counter(第一行)，b = Counter(第二行)，diff = a - b
#       diff 里剩下的是"多出来的"，用 sorted(diff.elements()) 拼成字符串
#       （elements() 会把每个字符按次数重复列出来）
# 自测：多出 1 个字符、多出好几个相同字符（如 aab 和 b，多出 a 和 a）

# ===== 你的代码写在这里 =====

# ===========================