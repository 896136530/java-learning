# 题 5：有序表的查找与插入（bisect）
# 输入：第一行一个整数 n；第二行 n 个整数（未排序）；第三行一个整数 x
# 输出：两行
#   第 1 行：把 n 个数排序后，x 应该插入的下标位置（0 开始，左边优先，即"插在相同元素前面"）
#   第 2 行：插入 x 之后，整个列表（保持升序，数字之间用空格隔开）
# 提示：from bisect import bisect_left, insort
#       先 nums.sort()（bisect 用在没排序的表上结果全错！）
#       i = bisect_left(nums, x) 拿位置；insort(nums, x) 插入并保持有序
# 自测：x 比所有数都小（位置 0）、x 比所有数都大（位置 = n）、x 等于表里已有的数（插在它前面，注意这时列表里会出现两个相同的数）

# ===== 你的代码写在这里 =====

# ===========================