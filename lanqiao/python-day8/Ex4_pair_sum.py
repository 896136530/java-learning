# 题 4：两两配对凑目标（itertools.combinations）
# 输入：第一行一个整数 n；第二行 n 个互不相同的整数；第三行一个整数 target
# 输出：一行 —— 从这 n 个数里任选 2 个数，有多少对的和正好等于 target
# 提示：from itertools import combinations
#       for x, y in combinations(nums, 2): 如果 x + y == target 就计数
#       ⚠️ combinations 只按"下标组合"，顺序不同的同一对只算一次（正好符合题意）
# 自测：n=1（没有任何对 → 0）、凑不出任何一对（→ 0）、刚好有一对

# ===== 你的代码写在这里 =====

# ===========================