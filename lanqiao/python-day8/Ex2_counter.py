# 题 2：字符统计（Counter）
# 输入：一行小写字母组成的字符串（可能含空格，空格不算）
# 输出：两行
#   第 1 行：出现次数最多的字符及其次数，格式 字符 次数
#   第 2 行：出现过的不同字符个数
# 提示：from collections import Counter
#       Counter(s) 直接统计；most_common(1) 取最多的那个
#       ⚠️ 空格不要算进去（可以用 s.replace(" ", "") 或只在非空格字符上统计）
# 自测：长度 1、全部字符相同、次数并列最多（看输出是哪个字符）

# ===== 你的代码写在这里 =====

# ===========================